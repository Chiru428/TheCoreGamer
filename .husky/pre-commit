#!/bin/sh
# Block accidental .env commits
if git diff --cached --name-only | grep -qE '(^|\/)\.env($|\.[^e]|\.local)'; then
  echo "ERROR: Attempting to commit a .env file."
  echo "Remove it with: git rm --cached <filename>"
  exit 1
fi
