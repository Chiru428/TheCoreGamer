{
  "name": "thecoregamer",
  "version": "1.0.0",
  "description": "## Prerequisites - Node.js 20+ - PostgreSQL database (Supabase recommended) - Redis (Upstash recommended for serverless, or local Redis for dev)",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepare": "husky install",
    "workers:start": "npm run workers:start --prefix backend",
    "workers:stop": "npm run workers:stop --prefix backend",
    "workers:restart": "npm run workers:restart --prefix backend",
    "workers:logs": "npm run workers:logs --prefix backend"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "husky": "^9.1.7"
  },
  "dependencies": {
    "@babel/parser": "^7.29.7"
  }
}
